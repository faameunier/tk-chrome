const SCORER = 'default';
const MAXIMUM_SCORE = 1000000000000000;
const MAXIMUM_HISTORY_SIZE = 1000;
const SESSIONS_TIMEOUT_MS = 1000;
const SESSIONS_MAX_FUZZY_DELTA_MS = 1000 * 5;

export { SCORER, MAXIMUM_SCORE, MAXIMUM_HISTORY_SIZE, SESSIONS_TIMEOUT_MS, SESSIONS_MAX_FUZZY_DELTA_MS };
